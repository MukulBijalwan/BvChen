## cran-comments.md

* This is a new submission.
* All examples run in < 5 s; no file system writes, no internet access,
  no external software is invoked.
* Tested locally on Windows (R 4.5.3) with `R CMD check --as-cran`;
  rhub/GitHub Actions runs are recommended before submission.
* There are no reverse dependencies (new package).
