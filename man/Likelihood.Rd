% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bvch_inference.R
\name{Likelihood}
\alias{Likelihood}
\alias{profile_beta}
\alias{loglik_bvch}
\title{Log-Likelihood and Profile Likelihood of the Bivariate Chen Model}
\usage{
profile_beta(
  z1,
  z2,
  betas = NULL,
  start = c(alpha1 = 1, alpha2 = 1, alpha3 = 1)
)

loglik_bvch(z1, z2, alpha1, alpha2, alpha3, beta)
}
\arguments{
\item{z1, z2}{numeric vectors of paired observations
(\code{z1[i]}, \code{z2[i]}).}

\item{betas}{candidate beta values for the profile grid; a sensible
default grid over (0, 20] is used if omitted.}

\item{start}{named numeric vector with starting values for the
alpha search inside \code{profile_beta}.}

\item{alpha1, alpha2, alpha3}{positive shape parameters of the three
latent Chen variables.}

\item{beta}{positive common power parameter.}
}
\value{
\code{loglik_bvch} returns the scalar log-likelihood;
  \code{profile_beta} returns a list with the evaluated grid
  (\code{beta}, \code{prof_loglik}) and the optimiser
  \code{beta_hat}.
}
\description{
Observed-data log-likelihood and profile likelihood for
  the common power parameter \eqn{\beta}.
}
\examples{
set.seed(7)
sim <- rbvch(300, 1, 1, 1, 1.5)
loglik_bvch(sim[, "z1"], sim[, "z2"], 1, 1, 1, 1.5)

pb <- profile_beta(sim[, "z1"], sim[, "z2"])
pb$beta_hat                        # close to 1.5
head(pb$grid)
}
